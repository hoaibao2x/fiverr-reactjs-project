const initialState = {
    listjob:[]
}



export const ListJobByNameReducer =  (state = initialState, action) => {
  switch (action.type) {

//   case first:
//     return { ...state }

  default:
    return state
  }
}
